# Crystal Voice Server Dependencies
# Install with: pip install -r requirements.txt

# Whisper STT
faster-whisper>=1.0.0
ctranslate2>=4.0.0

# TTS (choose one)
kokoro>=0.9.2  # Recommended - high quality neural TTS
# pyttsx3>=2.90  # Fallback - system TTS

# Server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# Audio processing  
soundfile>=0.12.0
numpy>=1.24.0
